/*
Deployment script for DBTier3

This code was generated by a tool.
Changes to this file may cause incorrect behavior and will be lost if
the code is regenerated.
*/

GO
SET ANSI_NULLS, ANSI_PADDING, ANSI_WARNINGS, ARITHABORT, CONCAT_NULL_YIELDS_NULL, QUOTED_IDENTIFIER ON;

SET NUMERIC_ROUNDABORT OFF;


GO
:setvar DatabaseName "DBTier3"
:setvar DefaultFilePrefix "DBTier3"
:setvar DefaultDataPath "C:\Users\Tibor Fekete\AppData\Local\Microsoft\Microsoft SQL Server Local DB\Instances\MSSQLLocalDB"
:setvar DefaultLogPath "C:\Users\Tibor Fekete\AppData\Local\Microsoft\Microsoft SQL Server Local DB\Instances\MSSQLLocalDB"

GO
:on error exit
GO
/*
Detect SQLCMD mode and disable script execution if SQLCMD mode is not supported.
To re-enable the script after enabling SQLCMD mode, execute the following:
SET NOEXEC OFF; 
*/
:setvar __IsSqlCmdEnabled "True"
GO
IF N'$(__IsSqlCmdEnabled)' NOT LIKE N'True'
    BEGIN
        PRINT N'SQLCMD mode must be enabled to successfully execute this script.';
        SET NOEXEC ON;
    END


GO
USE [$(DatabaseName)];


GO
/*
The type for column address in table [dbo].[AddressTable] is currently  CHAR (10) NOT NULL but is being changed to  CHAR (4) NOT NULL. Data loss could occur.
*/

IF EXISTS (select top 1 1 from [dbo].[AddressTable])
    RAISERROR (N'Rows were detected. The schema update is terminating because data loss might occur.', 16, 127) WITH NOWAIT

GO
/*
The type for column address in table [dbo].[EventTable] is currently  CHAR (10) NOT NULL but is being changed to  CHAR (4) NOT NULL. Data loss could occur.

The type for column ageLimit in table [dbo].[EventTable] is currently  NCHAR (10) NULL but is being changed to  VARCHAR (50) NULL. Data loss could occur.

The type for column description in table [dbo].[EventTable] is currently  NCHAR (10) NULL but is being changed to  VARCHAR (50) NULL. Data loss could occur.

The type for column endTime in table [dbo].[EventTable] is currently  NCHAR (10) NOT NULL but is being changed to  VARCHAR (50) NOT NULL. Data loss could occur.

The type for column entertainment in table [dbo].[EventTable] is currently  NCHAR (10) NULL but is being changed to  VARCHAR (50) NULL. Data loss could occur.

The type for column guests in table [dbo].[EventTable] is currently  NCHAR (10) NOT NULL but is being changed to  INT NOT NULL. Data loss could occur.

The type for column pets in table [dbo].[EventTable] is currently  NCHAR (10) NULL but is being changed to  VARCHAR (50) NULL. Data loss could occur.

The type for column startTime in table [dbo].[EventTable] is currently  NCHAR (10) NOT NULL but is being changed to  VARCHAR (50) NOT NULL. Data loss could occur.

The type for column userId in table [dbo].[EventTable] is currently  CHAR (10) NOT NULL but is being changed to  CHAR (4) NOT NULL. Data loss could occur.

The type for column welcomeFee in table [dbo].[EventTable] is currently  NCHAR (10) NULL but is being changed to  INT NULL. Data loss could occur.
*/

IF EXISTS (select top 1 1 from [dbo].[EventTable])
    RAISERROR (N'Rows were detected. The schema update is terminating because data loss might occur.', 16, 127) WITH NOWAIT

GO
/*
The type for column address in table [dbo].[UserTable] is currently  CHAR (10) NULL but is being changed to  CHAR (4) NULL. Data loss could occur.

The type for column age in table [dbo].[UserTable] is currently  NCHAR (10) NOT NULL but is being changed to  INT NOT NULL. Data loss could occur.

The type for column email in table [dbo].[UserTable] is currently  NCHAR (10) NOT NULL but is being changed to  VARCHAR (50) NOT NULL. Data loss could occur.

The type for column fName in table [dbo].[UserTable] is currently  NCHAR (10) NOT NULL but is being changed to  VARCHAR (50) NOT NULL. Data loss could occur.

The type for column lName in table [dbo].[UserTable] is currently  NCHAR (10) NOT NULL but is being changed to  VARCHAR (50) NOT NULL. Data loss could occur.

The type for column userId in table [dbo].[UserTable] is currently  CHAR (10) NOT NULL but is being changed to  CHAR (4) NOT NULL. Data loss could occur.
*/

IF EXISTS (select top 1 1 from [dbo].[UserTable])
    RAISERROR (N'Rows were detected. The schema update is terminating because data loss might occur.', 16, 127) WITH NOWAIT

GO
PRINT N'Dropping [dbo].[FK_EventTable_AddressTable]...';


GO
ALTER TABLE [dbo].[EventTable] DROP CONSTRAINT [FK_EventTable_AddressTable];


GO
PRINT N'Dropping [dbo].[FK_UserTable_AddressTable]...';


GO
ALTER TABLE [dbo].[UserTable] DROP CONSTRAINT [FK_UserTable_AddressTable];


GO
PRINT N'Dropping [dbo].[FK_EventTable_UserTable]...';


GO
ALTER TABLE [dbo].[EventTable] DROP CONSTRAINT [FK_EventTable_UserTable];


GO
PRINT N'Starting rebuilding table [dbo].[AddressTable]...';


GO
BEGIN TRANSACTION;

SET TRANSACTION ISOLATION LEVEL SERIALIZABLE;

SET XACT_ABORT ON;

CREATE TABLE [dbo].[tmp_ms_xx_AddressTable] (
    [address]  CHAR (4)   NOT NULL,
    [city]     NCHAR (10) NULL,
    [street]   NCHAR (10) NULL,
    [streetNr] NCHAR (10) NULL,
    [doorSide] NCHAR (10) NULL,
    [postCode] NCHAR (10) NULL,
    PRIMARY KEY CLUSTERED ([address] ASC)
);

IF EXISTS (SELECT TOP 1 1 
           FROM   [dbo].[AddressTable])
    BEGIN
        INSERT INTO [dbo].[tmp_ms_xx_AddressTable] ([address], [city], [street], [streetNr], [doorSide], [postCode])
        SELECT   [address],
                 [city],
                 [street],
                 [streetNr],
                 [doorSide],
                 [postCode]
        FROM     [dbo].[AddressTable]
        ORDER BY [address] ASC;
    END

DROP TABLE [dbo].[AddressTable];

EXECUTE sp_rename N'[dbo].[tmp_ms_xx_AddressTable]', N'AddressTable';

COMMIT TRANSACTION;

SET TRANSACTION ISOLATION LEVEL READ COMMITTED;


GO
PRINT N'Altering [dbo].[EventTable]...';


GO
ALTER TABLE [dbo].[EventTable] ALTER COLUMN [address] CHAR (4) NOT NULL;

ALTER TABLE [dbo].[EventTable] ALTER COLUMN [ageLimit] VARCHAR (50) NULL;

ALTER TABLE [dbo].[EventTable] ALTER COLUMN [description] VARCHAR (50) NULL;

ALTER TABLE [dbo].[EventTable] ALTER COLUMN [endTime] VARCHAR (50) NOT NULL;

ALTER TABLE [dbo].[EventTable] ALTER COLUMN [entertainment] VARCHAR (50) NULL;

ALTER TABLE [dbo].[EventTable] ALTER COLUMN [guests] INT NOT NULL;

ALTER TABLE [dbo].[EventTable] ALTER COLUMN [pets] VARCHAR (50) NULL;

ALTER TABLE [dbo].[EventTable] ALTER COLUMN [startTime] VARCHAR (50) NOT NULL;

ALTER TABLE [dbo].[EventTable] ALTER COLUMN [userId] CHAR (4) NOT NULL;

ALTER TABLE [dbo].[EventTable] ALTER COLUMN [welcomeFee] INT NULL;


GO
PRINT N'Starting rebuilding table [dbo].[UserTable]...';


GO
BEGIN TRANSACTION;

SET TRANSACTION ISOLATION LEVEL SERIALIZABLE;

SET XACT_ABORT ON;

CREATE TABLE [dbo].[tmp_ms_xx_UserTable] (
    [userId]  CHAR (4)     NOT NULL,
    [fName]   VARCHAR (50) NOT NULL,
    [lName]   VARCHAR (50) NOT NULL,
    [email]   VARCHAR (50) NOT NULL,
    [age]     INT          NOT NULL,
    [address] CHAR (4)     NULL,
    PRIMARY KEY CLUSTERED ([userId] ASC)
);

IF EXISTS (SELECT TOP 1 1 
           FROM   [dbo].[UserTable])
    BEGIN
        INSERT INTO [dbo].[tmp_ms_xx_UserTable] ([userId], [fName], [lName], [email], [age], [address])
        SELECT   [userId],
                 [fName],
                 [lName],
                 [email],
                 [age],
                 [address]
        FROM     [dbo].[UserTable]
        ORDER BY [userId] ASC;
    END

DROP TABLE [dbo].[UserTable];

EXECUTE sp_rename N'[dbo].[tmp_ms_xx_UserTable]', N'UserTable';

COMMIT TRANSACTION;

SET TRANSACTION ISOLATION LEVEL READ COMMITTED;


GO
PRINT N'Creating [dbo].[FK_EventTable_AddressTable]...';


GO
ALTER TABLE [dbo].[EventTable] WITH NOCHECK
    ADD CONSTRAINT [FK_EventTable_AddressTable] FOREIGN KEY ([address]) REFERENCES [dbo].[AddressTable] ([address]);


GO
PRINT N'Creating [dbo].[FK_UserTable_AddressTable]...';


GO
ALTER TABLE [dbo].[UserTable] WITH NOCHECK
    ADD CONSTRAINT [FK_UserTable_AddressTable] FOREIGN KEY ([address]) REFERENCES [dbo].[AddressTable] ([address]);


GO
PRINT N'Creating [dbo].[FK_EventTable_UserTable]...';


GO
ALTER TABLE [dbo].[EventTable] WITH NOCHECK
    ADD CONSTRAINT [FK_EventTable_UserTable] FOREIGN KEY ([userId]) REFERENCES [dbo].[UserTable] ([userId]);


GO
PRINT N'Checking existing data against newly created constraints';


GO
USE [$(DatabaseName)];


GO
ALTER TABLE [dbo].[EventTable] WITH CHECK CHECK CONSTRAINT [FK_EventTable_AddressTable];

ALTER TABLE [dbo].[UserTable] WITH CHECK CHECK CONSTRAINT [FK_UserTable_AddressTable];

ALTER TABLE [dbo].[EventTable] WITH CHECK CHECK CONSTRAINT [FK_EventTable_UserTable];


GO
PRINT N'Update complete.';


GO
